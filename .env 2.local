# =============================================================================
# ONYX Environment Variables - Development
# =============================================================================
# Development environment configuration
# NEVER commit actual secrets to version control
# =============================================================================
#
# SECURITY NOTES:
# =================
# 1. These are DEVELOPMENT values only
# 2. For production, use Docker secrets or external secret management
# 3. Generate strong, unique passwords for production
# 4. Use a 32-byte hex string for ENCRYPTION_KEY in production
# 5. Generate a secure SESSION_SECRET in production
# 6. Never use these placeholder values in production
# =============================================================================

# Google OAuth2 Configuration
GOOGLE_CLIENT_ID=dev-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=dev-google-client-secret-placeholder
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/callback/google

# LLM API Keys - DEV PLACEHOLDERS ONLY
TOGETHER_API_KEY=dev-together-api-key-placeholder
DEEPSEEK_API_KEY=dev-deepseek-api-key-placeholder
DEEPSEEK_MODEL=together/deepseek-chat
OLLAMA_BASE_URL=http://ollama:11434
OPENAI_API_KEY=dev-openai-api-key-placeholder

# Web Search API Keys - DEV PLACEHOLDERS ONLY
SERPAPI_API_KEY=dev-serpapi-api-key-placeholder
EXA_API_KEY=dev-exa-api-key-placeholder

# Database Configuration - DEV VALUES ONLY
POSTGRES_PASSWORD=dev-secure-password-12345
POSTGRES_DB=manus
POSTGRES_USER=manus
# DATABASE_URL constructed programmatically to avoid password exposure in config
# DATABASE_URL=postgresql://manus:dev-secure-password-12345@postgres:5432/manus

# Redis Configuration
REDIS_URL=redis://redis:6379
REDIS_PASSWORD=

# Qdrant Vector Database
QDRANT_URL=http://qdrant:6333
QDRANT_API_KEY=

# Encryption - DEV KEY ONLY (generate real one for production)
ENCRYPTION_KEY=1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef

# Session Secret - DEV SECRET ONLY (generate real one for production)
SESSION_SECRET=dev-session-secret-1234567890

# LiteLLM Proxy
LITELLM_URL=http://litellm-proxy:4000

# Monitoring - DEV PASSWORD ONLY
GRAFANA_PASSWORD=dev-grafana-password-12345

# Feature Flags
ENABLE_WORKSPACE=true
ENABLE_CODE_EXECUTION=true
ENABLE_GOOGLE_DRIVE=true
ENABLE_SLACK=true

# Development Environment Settings
NODE_ENV=development
LOG_LEVEL=info
LOG_FORMAT=json