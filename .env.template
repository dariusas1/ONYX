# =============================================================================
# ONYX Environment Variables Template
# =============================================================================
# This file contains variable references that will be substituted at runtime
# Use this as a template for creating environment-specific .env files
# =============================================================================

# Google OAuth2 Configuration
GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
GOOGLE_REDIRECT_URI=${GOOGLE_REDIRECT_URI:-http://localhost:3000/api/auth/callback/google}

# LLM API Keys
TOGETHER_API_KEY=${TOGETHER_API_KEY}
DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}

# Database Configuration
POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
DATABASE_URL=postgresql://manus:${POSTGRES_PASSWORD}@postgres:5432/manus

# Redis Configuration
REDIS_URL=${REDIS_URL:-redis://redis:6379}
REDIS_PASSWORD=${REDIS_PASSWORD}

# Qdrant Vector Database
QDRANT_URL=${QDRANT_URL:-http://qdrant:6333}
QDRANT_API_KEY=${QDRANT_API_KEY}

# Encryption
ENCRYPTION_KEY=${ENCRYPTION_KEY}

# LiteLLM Proxy
LITELLM_URL=${LITELLM_URL:-http://litellm-proxy:4000}

# Monitoring
GRAFANA_PASSWORD=${GRAFANA_PASSWORD}

# Session Management
SESSION_SECRET=${SESSION_SECRET}

# Feature Flags
ENABLE_WORKSPACE=${ENABLE_WORKSPACE:-true}
ENABLE_CODE_EXECUTION=${ENABLE_CODE_EXECUTION:-true}
ENABLE_GOOGLE_DRIVE=${ENABLE_GOOGLE_DRIVE:-true}
ENABLE_SLACK=${ENABLE_SLACK:-true}

# Development Settings
NODE_ENV=${NODE_ENV:-development}
PYTHONPATH=${PYTHONPATH:-/app}

# SSL Configuration (for production)
SSL_CERT_PATH=${SSL_CERT_PATH:-./nginx/ssl/cert.pem}
SSL_KEY_PATH=${SSL_KEY_PATH:-./nginx/ssl/key.pem}

# Backup Configuration
BACKUP_SCHEDULE=${BACKUP_SCHEDULE:-0 2 * * *}
BACKUP_RETENTION_DAYS=${BACKUP_RETENTION_DAYS:-30}

# Logging Configuration
LOG_LEVEL=${LOG_LEVEL:-info}
LOG_FORMAT=${LOG_FORMAT:-json}

# Rate Limiting
RATE_LIMIT_REQUESTS_PER_MINUTE=${RATE_LIMIT_REQUESTS_PER_MINUTE:-1000}
RATE_LIMIT_WINDOW_MS=${RATE_LIMIT_WINDOW_MS:-60000}

# Session Management
SESSION_TIMEOUT_MS=${SESSION_TIMEOUT_MS:-1800000}

# API Configuration
API_TIMEOUT_MS=${API_TIMEOUT_MS:-30000}
API_RETRY_ATTEMPTS=${API_RETRY_ATTEMPTS:-3}
API_RETRY_DELAY_MS=${API_RETRY_DELAY_MS:-1000}

# File Upload Limits
MAX_FILE_SIZE_MB=${MAX_FILE_SIZE_MB:-50}
ALLOWED_FILE_TYPES=${ALLOWED_FILE_TYPES:-pdf,doc,docx,txt,md}

# Background Jobs
BULLMQ_CONCURRENCY=${BULLMQ_CONCURRENCY:-5}
SYNC_INTERVAL_MINUTES=${SYNC_INTERVAL_MINUTES:-10}

# Docker Configuration
COMPOSE_PROJECT_NAME=${COMPOSE_PROJECT_NAME:-onyx}
COMPOSE_FILE=${COMPOSE_FILE:-docker-compose.yaml}

# Development Overrides
COMPOSE_OVERRIDE_FILE=${COMPOSE_OVERRIDE_FILE:-docker-compose.override.yml}

# =============================================================================
# Template Usage Notes:
# =============================================================================
# - Variables with ${VAR_NAME} will be substituted at runtime
# - Variables with ${VAR_NAME:-default} have default values if not provided
# - Create actual .env files by copying this template and filling in values
# - Never commit actual .env files with real secrets to version control
# =============================================================================