# =============================================================================
# ONYX Environment Variables Template
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to version control
# =============================================================================

# Google OAuth2 Configuration
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/callback/google

# LLM API Keys
TOGETHER_API_KEY=your-together-ai-api-key
DEEPSEEK_API_KEY=your-deepseek-api-key
DEEPSEEK_MODEL=together/deepseek-chat
OLLAMA_BASE_URL=http://ollama:11434
OPENAI_API_KEY=your-openai-api-key-here

# Web Search API Keys (Epic 7: Web Automation & Search)
SERPAPI_API_KEY=your-serpapi-api-key-here
EXA_API_KEY=your-exa-api-key-here

# Database Configuration
POSTGRES_PASSWORD=generate-a-secure-random-password-here
POSTGRES_DB=manus
POSTGRES_USER=manus
DATABASE_URL=postgresql://manus:${POSTGRES_PASSWORD}@postgres:5432/manus

# Redis Configuration
REDIS_URL=redis://redis:6379
REDIS_PASSWORD=optional-redis-password

# Qdrant Vector Database
QDRANT_URL=http://qdrant:6333
QDRANT_API_KEY=  # Optional - leave empty for development

# Encryption
ENCRYPTION_KEY=generate-32-byte-hex-string-for-aes-256

# LiteLLM Proxy
LITELLM_URL=http://litellm-proxy:4000

# Monitoring
GRAFANA_PASSWORD=secure-grafana-admin-password

# Feature Flags
ENABLE_WORKSPACE=true
ENABLE_CODE_EXECUTION=true
ENABLE_GOOGLE_DRIVE=true
ENABLE_SLACK=true

# Development Settings
NODE_ENV=development
PYTHONPATH=/app

# SSL Configuration (for production)
SSL_CERT_PATH=./nginx/ssl/cert.pem
SSL_KEY_PATH=./nginx/ssl/key.pem

# Backup Configuration
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=30

# Logging Configuration
LOG_LEVEL=info
LOG_FORMAT=json

# Rate Limiting
RATE_LIMIT_REQUESTS_PER_MINUTE=100
RATE_LIMIT_WINDOW_MS=60000

# Session Management
SESSION_SECRET=generate-secure-session-secret
SESSION_TIMEOUT_MS=1800000

# API Configuration
API_TIMEOUT_MS=30000
API_RETRY_ATTEMPTS=3
API_RETRY_DELAY_MS=1000

# File Upload Limits
MAX_FILE_SIZE_MB=50
ALLOWED_FILE_TYPES=pdf,doc,docx,txt,md

# Background Jobs
BULLMQ_CONCURRENCY=5
SYNC_INTERVAL_MINUTES=10

# Docker Configuration
COMPOSE_PROJECT_NAME=onyx
COMPOSE_FILE=docker-compose.yaml

# Development Overrides
COMPOSE_OVERRIDE_FILE=docker-compose.override.yml

# =============================================================================
# Security Notes:
# =============================================================================
# 1. Generate strong, unique passwords for all *_PASSWORD fields
# 2. Use a 32-byte hex string for ENCRYPTION_KEY (openssl rand -hex 32)
# 3. Generate a secure SESSION_SECRET (openssl rand -base64 32)
# 4. Never commit actual values to version control
# 5. Use different values for development vs production
# 6. Rotate secrets regularly in production
# =============================================================================

# =============================================================================
# Quick Generation Commands:
# =============================================================================
# Generate encryption key: openssl rand -hex 32
# Generate session secret: openssl rand -base64 32
# Generate postgres password: openssl rand -base64 32
# =============================================================================